# PDD prompt sections

Add `.md` files named by section id (e.g. `process_summary.md`, `to_be_hint.md`) and load them via `load_document_prompt("pdd", "<section_id>")` from builder code when you split PDD copy out of Python.
