# SOP prompt sections

Add `.md` files per section (e.g. `purpose.md`, `scope.md`) and wire `load_document_prompt("sop", "<section_id>")` in `sop/context_builder.py` as you migrate strings out of code.
